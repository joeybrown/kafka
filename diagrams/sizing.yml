# Diagram sizing configuration
# Each diagram can have custom width settings (height is calculated automatically)
diagrams:
  kafka-sequence:
    width: 800
  system-integration:
    width: 1200
  partition-key:
    width: 1000
  high-availability:
    width: 1200
  high-consistency:
    width: 1200
